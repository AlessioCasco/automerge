name: build_and_push_pretags

on:
  push:
    tags:
      - 'v*-pre*'

env:
  REGISTRY_IMAGE: alessiocasco/automerge

jobs:
  ruff:
    uses: ./.github/workflows/units/ruff.yml
  python-tests:
    uses: ./.github/workflows/units/python_tests.yml
  docker-build-and-push:
    uses: ./.github/workflows/units/build_and_push.yml
    